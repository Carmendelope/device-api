parameters:
  kubeconfig: "$HOME/.kube/config"
  platform: ""

steps:
  - script: |
      kubectl --kubeconfig ${{ parameters.kubeconfig }} create -f .
    workingDirectory: dist/yaml/${{ parameters.platform }}
    displayName: Create kubernetes resources
